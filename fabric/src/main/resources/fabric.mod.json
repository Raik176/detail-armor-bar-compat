{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {

  },
  "license": "${license}",
  "icon": "assets/${id}/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "${group}.${id}.fabric.DetailArmorBarCompatFabric"
    ]
  },
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}",
    "detailab": "*"
  },
  "mixins": [
    "${id}-fabric.mixins.json"
  ]
}